import { MoveToCenterDirective } from './move-to-center.directive';

describe('MoveToCenterDirective', () => {
  it('should create an instance', () => {
    const directive = new MoveToCenterDirective();
    expect(directive).toBeTruthy();
  });
});
