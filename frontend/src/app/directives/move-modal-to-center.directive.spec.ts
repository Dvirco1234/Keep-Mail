import { MoveModalToCenterDirective } from './move-modal-to-center.directive';

describe('MoveModalToCenterDirective', () => {
  it('should create an instance', () => {
    const directive = new MoveModalToCenterDirective();
    expect(directive).toBeTruthy();
  });
});
